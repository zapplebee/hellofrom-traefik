http:
  routers:
    hellofrom-live:
      rule: Host(`hellofrom.app`)
      entryPoints:
        - websecure
      tls:
        certResolver: le
      middlewares:
        - hf-route-blue@docker
      service: api@internal
